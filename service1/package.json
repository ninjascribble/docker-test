{
  "name": "docker-test_service1",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm install --production; node index.js",
    "dev": "npm install; nodemon --debug --harmony --verbose --exitcrash index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.13.3"
  },
  "devDependencies": {
    "nodemon": "^1.4.1"
  }
}
